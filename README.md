# Chandru E-Commerce — AKS + ACR + Terraform + CI/CD

## Overview
Production-grade demo: product microservice (Flask) deployed to AKS. Infrastructure is provisioned with Terraform; images stored in ACR. CI/CD via GitHub Actions.

## Quick steps (from scratch)

1. Clone:
   ```bash
   git clone <repo-url>
   cd chandru-ecommerce-aks/terraform
